### mobx一些注意点

#### action修饰器
    1，批处理提升性能，包裹在内的observable属性批量修改时只触发一次更新，如果没有action单独触发，则每次都触发更新

#### compute修饰器
    1，用于从现有的observable状态计算出新值
    2，缓存机制，只有依赖的observable状态变化时才会重新计算，如果没变，则返回上次结果
    3，依赖追踪，不需要指定依赖，会自动探测读取的observable状态
    4，如果compute值没有被其他副作用使用，则不会执行计算
    5，如果重新计算的值没变，则不会通知订阅者
