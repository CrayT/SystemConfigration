name: build
on: 
  push:
    branches:
      - master

jobs:
  buildJob:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Build
        run:
          pip install mkdocs 
          pip install mkdocs-material
      - name: Deploy
        run:
          git pull
          mkdocs gh-deploy --clean
